#%RAML 1.0 Trait
description: |
  Standard way of sending JSON messages back,
  usually for reponses, i.e. "Task completed"

responses:
  422:
    body:
      application/json:
        type: !include ../schemas/message-return.raml
        example:
          message: A required field was missing
